# Branding: Add StatServe Logo

## Context
Replace the default ApplicationLogo with the provided StatServe SVG logo across the app.

## Changes
1. Update `ApplicationLogo.vue` with the provided SVG
2. Ensure it renders correctly in nav (AuthenticatedLayout) and guest pages (GuestLayout)
